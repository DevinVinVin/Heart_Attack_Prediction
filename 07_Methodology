Load the dataset and perform initial cleaning.

Replace missing or undefined values with median/mode as appropriate.

Conduct exploratory data analysis (EDA) using visualization tools.

Normalize the data using standardization (z-score method).

Split the data into training and testing sets (80:20 split).

Train a Random Forest Classifier model.

Evaluate the model using standard metrics.

Save the model and scaler for future predictions.

Implement inference on new, unseen data.
